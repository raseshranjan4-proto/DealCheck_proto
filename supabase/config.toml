project_id = "nggfbjwpdggrezhtasys"

[functions.daily-pipeline]
# JWT verification is OFF: the Supabase cron UI / net.http_post could not reliably send a
# valid project JWT. The function gates on the PIPELINE_TRIGGER_SECRET query param instead
# (see index.ts). Every caller — cron and the scripts — passes ?key=<secret>.
verify_jwt = false
