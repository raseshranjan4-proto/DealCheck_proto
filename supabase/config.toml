project_id = "nggfbjwpdggrezhtasys"

[functions.daily-pipeline]
# Keep JWT verification ON so the pipeline is not an open, cost-incurring endpoint.
# Callers (cron job, dry-run scripts) pass the service-role key as a Bearer token.
verify_jwt = true
